import * as utils from './utils';
import * as helpers from './helpers';
import * as hooks from './hooks';
import * as constants from './constants';

window.vendorUtils = {
  ...utils
};

window.vendorHelpers = {
  ...helpers
};

window.vendorHooks = {
  ...hooks
};

window.vendorConstants = {
  ...constants
};